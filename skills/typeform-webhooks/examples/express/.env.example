# Typeform webhook signing secret
# Set this on the webhook (Connect > Webhooks > Edit > Add a Secret) or via the
# Webhooks API, then paste the same value here.
TYPEFORM_WEBHOOK_SECRET=your_typeform_webhook_secret_here
