# Per-webhook signing secret from the form's Integrations -> Webhooks configuration.
# Optional in Tally, but strongly recommended: when set, requests include a
# Tally-Signature header this handler verifies. Leave unset only for local/dev
# testing of unsigned webhooks.
TALLY_SIGNING_SECRET=your_signing_secret
