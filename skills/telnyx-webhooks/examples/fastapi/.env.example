# Telnyx account public key (base64) for webhook signature verification.
# Mission Control → Account Settings → Keys & Credentials → Public Key.
# This is per-ACCOUNT (not per messaging profile).
TELNYX_PUBLIC_KEY=your_base64_public_key_here
