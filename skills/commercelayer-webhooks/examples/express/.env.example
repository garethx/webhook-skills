# Commerce Layer webhook shared secret.
# Returned in the response when you create the webhook (POST /api/webhooks).
COMMERCELAYER_SHARED_SECRET=your_webhook_shared_secret_here

# Optional: port the example server listens on (defaults to 3000)
# PORT=3000
