# Optional shared secret. Set this same value as `verificationToken` when
# creating the RingCentral subscription. RingCentral sends it as the
# Verification-Token header on every notification. Leave unset to skip the check.
RINGCENTRAL_VERIFICATION_TOKEN=your_verification_token
