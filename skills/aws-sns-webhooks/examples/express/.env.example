# Optional: restrict which SNS topic this endpoint trusts.
# If set, messages whose TopicArn does not match are rejected with 403.
# Leave unset to accept any topic (still signature-verified).
AWS_SNS_TOPIC_ARN=arn:aws:sns:us-east-1:123456789012:MyTopic

# Port the server listens on
PORT=3000
