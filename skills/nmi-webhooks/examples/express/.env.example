# NMI webhooks signing key (signs the Webhook-Signature header).
# Merchant Control Panel → Settings → Webhooks. NOT your gateway API/security key.
NMI_SIGNING_KEY=your_webhook_signing_key

# Port the server listens on
PORT=3000
