fastapi>=0.141.1
uvicorn>=0.34.0
python-dotenv>=1.0.1

# Official Google auth library: verifies the OIDC token Pub/Sub attaches.
# `requests` is the transport google.auth.transport.requests.Request needs.
google-auth>=2.56.3
requests>=2.32.5

# Testing
pytest>=9.1.1
httpx>=0.28.1
cryptography>=44.0.0
