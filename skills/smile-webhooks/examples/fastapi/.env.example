# Per-endpoint webhook secret (1-64 chars) set when you registered the webhook
# in the Smile developer portal or via POST /webhooks. Used as the HMAC-SHA512 key.
SMILE_WEBHOOK_SECRET=your_webhook_secret
