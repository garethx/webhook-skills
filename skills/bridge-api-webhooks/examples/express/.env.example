# Bridge API webhook signing secret (shown once when the webhook is created/rotated)
BRIDGE_WEBHOOK_SECRET=your_webhook_signing_secret_here

# Optional: port to listen on (defaults to 3000)
PORT=3000
