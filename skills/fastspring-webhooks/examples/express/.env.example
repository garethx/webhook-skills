# FastSpring HMAC SHA256 Secret
# Dashboard → Developer Tools → Webhooks → Configuration → HMAC SHA256 Secret
FASTSPRING_WEBHOOK_SECRET=your_hmac_sha256_secret

# Optional: port the server listens on (defaults to 3000)
PORT=3000
