# Vercel Drain signature secret (Team Settings > Drains > Edit)
VERCEL_LOG_DRAIN_SECRET=your_drain_signature_secret

# Verification token echoed on the x-vercel-verify response header
# (shown in the dashboard when you create the drain)
VERCEL_VERIFY=your_verification_token
