# Per-webhook signing key (top-right of the webhook's detail page in the Alchemy Notify dashboard)
ALCHEMY_SIGNING_KEY=your_webhook_signing_key_here

# Port the server listens on (optional, defaults to 3000)
PORT=3000
