# HTTP Basic Auth credentials agreed with the Ethoca Customer Delivery Team.
# Ethoca sends these on every alert as: Authorization: Basic base64(username:password)
ETHOCA_WEBHOOK_USERNAME=your_basic_auth_username
ETHOCA_WEBHOOK_PASSWORD=your_basic_auth_password
