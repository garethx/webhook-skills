# Synctera webhook signing secret from POST /v0/webhook_secrets
# This is NOT your API key.
SYNCTERA_WEBHOOK_SECRET=your_signature_secret_here

# Optional: port the server listens on (defaults to 3000)
PORT=3000
