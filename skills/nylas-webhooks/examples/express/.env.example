# Per-destination secret, returned when the Nylas webhook is created or its secret is rotated.
NYLAS_WEBHOOK_SECRET=your_webhook_secret_here

# Port the server listens on (optional, defaults to 3000)
PORT=3000
