# Signing key from the Reporting Webhooks integration page (Customer.io account settings)
CUSTOMERIO_WEBHOOK_SIGNING_KEY=your_signing_key

# Port the server listens on (optional, defaults to 3000)
PORT=3000
