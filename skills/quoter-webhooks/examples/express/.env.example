# Quoter "Hash Key" — the shared secret configured in Quoter under
# Settings -> Integrations. Optional in Quoter, but REQUIRED by this example.
# Always set a long, random value; a blank hash key means no verification.
QUOTER_HASH_KEY=your_hash_key_here

# Optional: port the server listens on (defaults to 3000)
PORT=3000
