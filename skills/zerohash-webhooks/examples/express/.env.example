# Zero Hash HMAC webhook shared secret (provisioned by your Zero Hash rep)
ZEROHASH_WEBHOOK_SECRET=your_zero_hash_hmac_shared_secret

# Port the server listens on (optional, defaults to 3000)
PORT=3000
