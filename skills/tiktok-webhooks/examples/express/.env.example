# Your TikTok app's client secret (from developers.tiktok.com → your app →
# Basic information). This signs the webhook — there is no separate webhook secret.
TIKTOK_CLIENT_SECRET=your_app_client_secret

# Port the server listens on
PORT=3000
