# The webhook signing secret provided by Ascend (email developers@useascend.com).
ASCEND_WEBHOOK_SECRET=your_ascend_webhook_secret

# Port the server listens on (optional, defaults to 3000).
PORT=3000
