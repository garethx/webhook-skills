# Airwallex webhook secret for THIS webhook URL
# Web app > Settings > Developer > Webhooks (each URL has its own secret)
AIRWALLEX_WEBHOOK_SECRET=whsec_your_webhook_secret_here

# Optional: port to listen on (defaults to 3000)
PORT=3000
