# Neon Auth domain — JWKS is fetched from ${NEON_AUTH_URL}/.well-known/jwks.json
# There is NO signing secret: Neon signs with Ed25519 and you verify with the public JWKS.
NEON_AUTH_URL=https://your-neon-auth-domain.com

# Optional: port for the server (defaults to 3000)
# PORT=3000
