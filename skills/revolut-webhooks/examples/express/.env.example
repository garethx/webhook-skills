# Revolut webhook signing secret (returned when you create the webhook via the
# Merchant API; starts with wsk_). See references/setup.md.
REVOLUT_SIGNING_SECRET=wsk_xxxxx

# Port the server listens on (optional, defaults to 3000)
PORT=3000
