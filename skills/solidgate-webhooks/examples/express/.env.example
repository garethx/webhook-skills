# Solidgate webhook keys (Hub > Developers). Separate from your API keys.
SOLIDGATE_WEBHOOK_PUBLIC_KEY=wh_pk_your_public_key_here   # sent as the "merchant" header
SOLIDGATE_WEBHOOK_SECRET_KEY=wh_sk_your_secret_key_here   # used to compute the HMAC

# Port the server listens on
PORT=3000
