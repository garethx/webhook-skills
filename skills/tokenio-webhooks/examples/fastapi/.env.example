# Your member's Ed25519 PUBLIC key (base64url, no padding) from the Token
# Dashboard → Settings → Member Information. This is NOT a shared secret.
TOKEN_WEBHOOK_PUBLIC_KEY=your_member_ed25519_public_key_base64url
