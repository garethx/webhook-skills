# The secret you set when creating the Picqer hook (POST /api/v1/hooks "secret" field)
PICQER_WEBHOOK_SECRET=your_hook_secret_here

# Optional: port for the Express server (defaults to 3000)
# PORT=3000
