# Akeneo connection secret (from Connect > Connection settings)
# Akeneo signs each request with HMAC-SHA256 using this secret.
AKENEO_WEBHOOK_SECRET=your_connection_secret

# Port the server listens on (optional, defaults to 3000)
PORT=3000
