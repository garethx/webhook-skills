fastapi>=0.139.2
uvicorn>=0.30.0
jwcrypto>=1.5.8
requests>=2.31.0

# Test dependencies
pytest>=9.1.1
httpx>=0.28.1

# Optional: the Fireblocks SDK manages webhook CONFIGURATION (create/list/resend).
# It does NOT provide signature verification — this example uses jwcrypto for that.
# Uncomment if you also manage webhooks programmatically:
# fireblocks>=23.0.0
