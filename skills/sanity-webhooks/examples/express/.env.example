# Sanity webhook signing secret (set when creating the webhook at sanity.io/manage)
SANITY_WEBHOOK_SECRET=your_webhook_secret_here

# Optional: port the server listens on (defaults to 3000)
PORT=3000
